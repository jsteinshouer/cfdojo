component extends="coldbox.system.EventHandler" singleton="true" {	property name="moduleService" inject="modules.ModuleService";	// Default Action	function index(event,rc,prc){		prc.modules = getSetting("modules");		event.setView("main/index");	}	/************************************** IMPLICIT ACTIONS *********************************************/	function onAppInit(event,rc,prc){		moduleService.setupDB();		moduleService.loadModules();	}	function onRequestStart(event,rc,prc){				if (len(event.getCurrentModule()) && event.getCurrentModule() != "about") {			prc.qKata = moduleService.getKata(event.getCurrentModule());		}	}	function onRequestEnd(event,rc,prc){	}	function onSessionStart(event,rc,prc){	}	function onSessionEnd(event,rc,prc){		var sessionScope = event.getValue("sessionReference");		var applicationScope = event.getValue("applicationReference");	}	function onException(event,rc,prc){		//Grab Exception From private request collection, placed by ColdBox Exception Handling		var exception = prc.exception;		//Place exception handler below:	}	function onMissingTemplate(event,rc,prc){		//Grab missingTemplate From request collection, placed by ColdBox		var missingTemplate = event.getValue("missingTemplate");	}}